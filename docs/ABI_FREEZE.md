# ABI Freeze

ABI reservations include object headers, vtables, class layout, symbol mangling, constructor/destructor symbols, exception boundaries, and platform alignment. GA must lock these contracts.
